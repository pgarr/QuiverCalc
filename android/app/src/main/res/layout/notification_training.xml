<?xml version="1.0" encoding="utf-8"?>
<!--
  Custom notification layout for active training.
  DecoratedCustomViewStyle adds the system header (app icon, name, time) above this view.
  The button row is always visible in the collapsed state — no expansion needed.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="2dp"
    android:paddingBottom="4dp">

    <TextView
        android:id="@+id/notification_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:paddingBottom="4dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btn_action_0"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:textSize="13sp"
            android:textAllCaps="false"
            android:padding="0dp"
            android:visibility="gone"/>

        <Button
            android:id="@+id/btn_action_1"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:textSize="13sp"
            android:textAllCaps="false"
            android:padding="0dp"
            android:visibility="gone"/>

        <Button
            android:id="@+id/btn_action_2"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:textSize="13sp"
            android:textAllCaps="false"
            android:padding="0dp"
            android:visibility="gone"/>

    </LinearLayout>
</LinearLayout>
